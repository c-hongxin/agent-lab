# agent-ui（阶段 2～3 使用）

可复用的 Agent UI 组件：

- `MessageList` / `ToolCallCard`（Demo A 暂在 `src/components/chat/`）
- `ApprovalCard`（Demo B）
- `NotificationPreviewCard`（Demo B/C）

阶段 2 可将组件从 `src/components/chat` 迁入此包。
