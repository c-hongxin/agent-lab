# 前端 Agent 工程学习方案

> **经验基础**：已有多年 React/TS 中后台经验（复杂交互、权限、请求治理、组件/Hooks 沉淀、工程化）；有 AI 辅助开发（生码/自测/CR）使用经验。本方案跳过 React 入门。  
> **练习选题（以当前业务为准）**：社区平台中的 **通知文案 / locale / 邮件模板 / 运营后台** 等可脱敏抽象的场景；不依赖、不复刻过往公司代码。  
> **学习目标**：掌握 Agent 协议、流式 UI、Tool Calling、HITL、Generative UI，并完成可运行的练习项目  
> **建议周期**：**6～10 周**（工作日 1～1.5h + 周末半天）

---

## 如何使用本文档

每一阶段都按同一结构阅读：

| 小节 | 含义 |
| ---- | ---- |
| **要读什么** | 链接 + 精读/扫过/预习 |
| **要做什么** | 可勾选的具体动作（写代码 / 写笔记 / 搭仓库） |
| **交付物** | 本阶段结束必须有的文件或能力 |
| **验收标准** | 自测：做到哪几条算过关 |
| **常见卡点** | 踩坑时先查这里 |

**原则**：先跑通官方示例，再在自己的练习仓库里实现 Demo；**不要**一上来就接 `web-community-platform` 真业务代码。

**练习仓库建议路径**（与当前业务仓库分开）：`C:\Users\HP\Desktop\agent-lab\`（名称自定，下文统称 **练习仓库**）。

---

## 0. 定位：经验定标准，当前业务定选题

| 来源           | 用法                                                                |
| -------------- | ------------------------------------------------------------------- |
| 既有中后台经验 | 定「熟手验收标准」：组件拆分、权限思维、稳定性、可量化              |
| 当前业务       | **Demo / 练习选题**：抽象通知·邮件·运营流程，用自造数据完成练习仓库 |

### 0.1 已具备（跳过）

| 能力             | 说明                                              |
| ---------------- | ------------------------------------------------- |
| React Hooks + TS | 直接上 AI SDK                                     |
| 复杂中后台       | 表单联动、权限、状态枚举、请求容错有体感          |
| 工程化           | 规范、分层、接口契约意识强                        |
| AI 辅助开发      | 会用工具；本方案升级为「会搭 Agent 能力」         |
| 当前域知识       | 通知 trigger、locale、邮件模板——**Demo C 主素材** |

### 0.2 要补齐（主战场）

| 缺口                      | 为什么重要                                 |
| ------------------------- | ------------------------------------------ |
| LLM / Tool Calling 协议   | 决定 Tool UI 状态机怎么设计                |
| 流式 UI Message / `parts` | 和 REST 列表页心智不同                     |
| HITL 确认卡               | Agent 与普通 Chat 的分水岭                 |
| Generative UI             | 模型驱动业务预览组件                       |
| Eval / 成本 / 观测        | 保证能力可落地、可回归                     |
| 练习项目闭环              | 用当前业务域脱敏 Demo 巩固，不依赖过往代码 |

### 0.3 一句话目标

> 在现有中后台与工程化基础上，用 AI SDK 落地「通知文案 Agent 工作台」练习：流式对话、Tool Calling、HITL 与评测闭环。

---

## 1. 培养目标

1. 流式聊天 UI，可 Stop / Retry
2. Tool Calling 状态机可复用
3. 写操作必须 HITL
4. Generative UI ≥ 1（**通知预览卡**优先）
5. 简单 eval（有数字）
6. **Demo C：通知文案 Agent 工作台**（贴合当前业务，脱敏 mock）

### 能力画像

| 能力       | 期望                                |
| ---------- | ----------------------------------- |
| Agent 协议 | 能画 tool-call ↔ tool-result 时序   |
| 前端工程   | Agent UI 模块化，不是单文件堆逻辑   |
| 产品体验   | 加载 / 失败 / 权限 / 确认           |
| 工程化     | Zod、eval、密钥边界                 |
| 判断力     | 哪些用 Agent，哪些继续表单/配置后台 |

---

## 2. 技术栈（按优先级）

| 优先级 | 技术                             | 投入策略     |
| ------ | -------------------------------- | ------------ |
| P0     | Vercel AI SDK                    | **主学**     |
| P0     | Zod                              | 补齐         |
| P0     | Next.js App Router               | **够用即可** |
| P1     | assistant-ui 或 AntD 自研 Thread | 提效         |
| P1     | MCP 基础                         | 加分         |
| P2     | LangGraph.js                     | 按需         |
| P2     | LangSmith / 自建 Eval            | 加分         |
| —      | React / TS 基础                  | **跳过**     |
| —      | 过往公司业务 / 内部组件复刻      | **不做**     |

### 2.1 投入说明

- **AI SDK（主学）**：流式 + Tool + `useChat`
- **Zod**：tool 入参 / 结构化输出（对齐字段校验思维）
- **Next.js**：Route Handler 即可
- **UI**：Ant Design（贴近运营后台）或 assistant-ui
- **MCP**：对照 Cursor 理解工具暴露方式
- **LangGraph**：多步审批再学

### 2.2 文档链接（优先中文）

- AI SDK 中文站首页: https://ai-sdk.com.cn/
- Zod 中文文档: https://zod.dev.cn/（若打不开可用 https://zod.dev/）
- Next.js App Router 中文: https://nextjs.net.cn/docs/app
- assistant-ui（暂无完整中文站，可对照英文）: https://www.assistant-ui.com/docs/
- MCP 中文介绍（社区）: https://www.learngraph.online/LearnGraph%201.X/module-11-subgraph-mermaid-mcp-agent-node-tool/11.3%20LangGraph%20MCP%20Integration.html
- LangGraph JS 中文: https://docs.langchain.org.cn/oss/javascript/langgraph/overview
- LangSmith（英文为主）: https://docs.smith.langchain.com/

> 说明：`ai-sdk.com.cn`、`openai.ac.cn`、`langchain.org.cn` 多为社区中文镜像/译本，内容偶有滞后；若与代码对不上，再对照官方英文页 https://ai-sdk.dev/ 。

### 2.3 选型建议

- **AI SDK 一条线打穿**，再按需 LangGraph
- 不要同时学多个 Agent 框架

### 2.4 练习仓库初始化（阶段 1 第 1 天做）

在 **练习仓库** 执行（模型与 Key 按你实际可用的填）：

```powershell
# 示例：Next.js + AI SDK
npx create-next-app@latest agent-lab --ts --eslint --app --src-dir --import-alias "@/*"
cd agent-lab
pnpm add ai @ai-sdk/openai zod
# 或 @ai-sdk/anthropic 等，与 .env 里 PROVIDER 一致
```

创建 `.env.local`（**勿提交 git**）：

```env
OPENAI_API_KEY=sk-...
# 或 ANTHROPIC_API_KEY 等
```

`.gitignore` 确认包含 `.env*.local`。README 里写一句：API Key 仅服务端 Route Handler 使用。

---

## 3. 阶段总览

| 阶段 | 时长          | 主题                         | 交付物            |
| ---- | ------------- | ---------------------------- | ----------------- |
| 0    | **1～2 天**   | 概念速通                     | 半页笔记          |
| 1    | **1～1.5 周** | 流式 Chat + Tool             | Demo A            |
| 2    | **1.5～2 周** | HITL + Generative UI         | Demo B            |
| 3    | **2.5～3 周** | 通知文案 Agent（当前业务域） | **Demo C**        |
| 4    | **1.5～2 周** | Eval / MCP / 文档收尾        | README + 评测数字 |

合计约 **6～10 周**。

**为何可压缩周期**：前端与中后台已熟；选题直接用当前通知/邮件认知；过往经验只提高验收标准，不提供代码依赖。

**阶段依赖关系**：

```text
阶段 0 笔记 → 阶段 1 Demo A（通用 Chat+Tool）
           → 阶段 2 Demo B（HITL + 预览卡）
           → 阶段 3 Demo C（通知业务域）
           → 阶段 4 Eval + MCP + 文档
```

---

## 4. 阶段 0：概念速通（1～2 天）

### 4.1 要读什么（中文优先）

| 顺序 | 材料 | 读法 | 链接 |
| ---- | ---- | ---- | ---- |
| 1 | Anthropic《构建有效的 Agent》 | 精读前半（模式分类 + 何时不用 Agent） | https://blog.iaieye.com/posts/agentic-coding-classics/anthropic-building-effective-agents-fulltext/ |
| 2 | OpenAI 函数调用 | 精读「调用流程」「参数 schema」 | https://developers.openai.ac.cn/api/docs/guides/function-calling |
| 3 | 工具调用与 MCP 通识 | 任选一篇扫完 | 火山引擎 / 博客园 / libresensing（见下） |
| 4 | MCP vs Function Calling | 扫过，建立名词 | https://cloud.tencent.com/developer/techpedia/2621/20674 |

通识备选：

- https://developer.volcengine.com/articles/7533551311816818724  
- https://www.cnblogs.com/pass-ion/p/19162781  
- https://libresensing.com/post/function-calling-and-mcp-practice  

英文原文（可选）: https://www.anthropic.com/engineering/building-effective-agents  

### 4.2 要做什么

- [ ] 用 **1～2 小时** 读完上表材料（不必背 API）
- [ ] 新建笔记文件：`notes/phase-0-concepts.md`（放练习仓库或个人 `资料/` 均可）
- [ ] 按下面 **4.3 模板** 写满 **半页～一页**（用自己的话，禁止大段粘贴译文）
- [ ] 画一张 **tool-call → tool-result** 时序（手绘拍照或 Mermaid 均可）

### 4.3 笔记模板（照此填空）

```markdown
# 阶段 0：Agent 概念笔记

## 1. LLM vs Tool vs Agent Loop
- LLM 单独能做什么：…
- Tool 解决什么：…
- Agent Loop（对照 Cursor）：用户消息 → 模型 → tool-call → 执行 → tool-result → 模型继续 → …

## 2. 前端为什么要处理 tool-call / tool-result
- tool-call：模型「打算调什么、参数是什么」
- tool-result：执行结果，必须回传，否则 loop 断掉
- UI 上要展示：pending / success / error（不是只 console.log）

## 3. 当前业务：Agent vs 运营表单 / 审核流

### 适合 Agent（辅助层）
- 通知文案起草、字段校验、多语言对齐、邮件占位符检查、预览
- 自然语言问「某 trigger 需要哪些 business_params」

### 必须留在表单 / 审核流（执行层）
- 审核通过/驳回、填写法定驳回原因、改订单/用户状态
- 真正发邮件/推通知（后端状态机 + 权限 + 审计）
- email_template 硬约束（驳回必须含原因、不暴露签到码等）

### 一句话边界
Agent 止于草稿与预览；人确认后仍走 B 端表单与 API。
```

### 4.4 交付物

| 交付物 | 说明 |
| ------ | ---- |
| `notes/phase-0-concepts.md` | 半页以上概念笔记 |
| 时序图 1 张 | tool-call / tool-result |

### 4.5 验收标准

- [ ] 能 **口头 2 分钟** 讲清 Agent Loop，不混淆「模型生成文本」和「工具执行」
- [ ] 能举 **1 个** 你们业务例子：赏金驳回 = Agent 写草稿 + 审核表单提交驳回
- [ ] 笔记里 **没有** 公司真实文案、用户数据、内部 URL

### 4.6 常见卡点

| 现象 | 处理 |
| ---- | ---- |
| 译文链接打不开 | 换备选链接或英文原文 + 浏览器翻译 |
| Agent 和 ChatGPT 分不清 | 记住：有 **工具循环 + 状态** 才是 Agent UI 要关心的 |

---

## 5. 阶段 1：流式 Chat + Tool（约 1～1.5 周）

**本阶段目标**：在 **练习仓库** 做出 **Demo A**——与业务无关的通用聊天页，但具备流式、Stop、双 tool、`parts` 渲染、组件拆分。  
**本阶段不做**：通知 trigger、HITL 确认卡、接公司仓库。

### 5.1 官方主线（读 + 做）

| 顺序 | 内容 | 读法 | 你要做的具体动作 | 链接 |
| ---- | ---- | ---- | ---------------- | ---- |
| 1 | 快速开始 | **当天跑通** | 初始化练习仓库；建 `app/api/chat/route.ts`；页面 `useChat` 流式出字 | [Getting Started](https://ai-sdk.com.cn/docs/getting-started) |
| 2 | Tools and Tool Calling | **精读** | 在 route 里加 **1 个服务端 tool**（如 `getWeather` 查假数据）；理解 `tools` / `execute` | [Tools](https://ai-sdk.com.cn/docs/ai-sdk-core/tools-and-tool-calling) |
| 3 | Chatbot UI | **扫过** | 弄清 `messages`、`status`、`append`；不纠结样式 | [Chatbot](https://ai-sdk.com.cn/docs/ai-sdk-ui/chatbot) |
| 4 | Chatbot Tool Usage | **精读 + 照做** | 按文档渲染 `message.parts`；做 `ToolCallCard` 显示 tool 名与参数 | [Tool Usage](https://ai-sdk.com.cn/docs/ai-sdk-ui/chatbot-tool-usage) |
| 5 | 结构化输出 | **与 Zod 一起看** | 给 tool 的 `parameters` 加 Zod schema；可选试一次 `generateObject` | [Structured Data](https://ai-sdk.com.cn/docs/ai-sdk-core/generating-structured-data) |
| 6 | HITL | **预习 only** | 浏览 cookbook，知道「写操作要人点确认」；**本阶段不实现** | [HITL](https://ai-sdk.com.cn/cookbook/next/human-in-the-loop) |

**补充（做到 Demo A 时用）**

| 主题 | 何时看 | 链接 |
| ---- | ------ | ---- |
| 停止流 / Abort | 做 Stop 按钮 | https://ai-sdk.com.cn/docs/advanced/stopping-streams |
| AbortController | 同上 | https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController |
| SSE | 流式卡住时 | https://developer.mozilla.org/zh-CN/docs/Web/API/Server-sent_events |
| Cookbook / Examples | 需要抄结构时 | https://ai-sdk.com.cn/cookbook 、 https://ai-sdk.com.cn/examples |

### 5.2 建议日程（工作日 1～1.5h/天）

| 天 | 任务 | 勾选 |
| -- | ---- | ---- |
| D1 | 2.4 初始化仓库 + Getting Started 流式对话 | [ ] |
| D2 | 精读 Tools 文档 + 服务端 tool 跑通 | [ ] |
| D3 | Chatbot Tool Usage 照做 + `parts` 文本/tool 分渲染 | [ ] |
| D4 | 客户端 tool 1 个（如 `showAlert` / 读 `window.innerWidth`）+ `ToolCallCard` | [ ] |
| D5 | Stop 按钮（`stop()` + Abort）+ 拆 `MessageList` | [ ] |
| D6 | Zod 校验 tool 入参 + 错误态 UI | [ ] |
| D7 | 写 Demo A README 草稿 + 对照 5.5 验收 | [ ] |

周末若有空：把 UI 稍微整理（输入框、发送、停止、清空会话）。

### 5.3 Demo A 规格（照此实现）

#### 5.3.1 功能清单

| # | 功能 | 要求 |
| - | ---- | ---- |
| 1 | 流式回复 | 用户发送后逐字/逐块显示 assistant 文本 |
| 2 | Stop | 流式过程中可中断，UI 有明确「已停止」态 |
| 3 | 服务端 tool | 在 Route Handler 里 `execute`，例如 `getWeather({ city })` 返回 JSON |
| 4 | 客户端 tool | 在浏览器 `onToolCall` 执行，例如 `getViewportSize` |
| 5 | `parts` 渲染 | 同一条 message 内：文本 part + tool part 分开渲染 |
| 6 | Tool 状态 UI | 至少区分：`input-streaming` / `call` / `result` / `error`（以 SDK 实际 state 为准） |
| 7 | 组件拆分 | 禁止全部写在 `page.tsx` |

#### 5.3.2 建议目录结构

```text
agent-lab/
  src/app/
    page.tsx                 # 薄组装：ChatPage
    api/chat/route.ts        # streamText + tools
  src/components/chat/
    ChatPage.tsx
    MessageList.tsx
    MessageItem.tsx
    ToolCallCard.tsx
    ChatInput.tsx
  src/lib/tools/
    server-tools.ts          # 服务端 tool 定义
    client-tools.ts          # 客户端 tool 类型/处理
  notes/phase-0-concepts.md
  README.md
```

#### 5.3.3 两个 tool 示例（可用同名，数据假即可）

**服务端 `getWeather`**

- 输入：`city: string`（Zod）
- 输出：`{ city, temperature, condition }` 假数据
- 用途：练习「模型决定调 API、服务端执行」

**客户端 `getViewportSize`**

- 输入：无或 `{}`
- 输出：`{ width, height }`
- 用途：练习「必须在前端执行、result 要 addToolOutput 回传」

#### 5.3.4 README 必须写的 3 段（Demo A）

1. **如何启动**：`pnpm install` / `pnpm dev` / 环境变量
2. **与普通 Chat 的差异**：`parts`、tool 状态、多轮 tool loop
3. **与 B 端列表页的差异**：消息流驱动 UI，不是一次 `request` 拿全量 `data.list`

### 5.4 交付物

| 交付物 | 说明 |
| ------ | ---- |
| 练习仓库可运行 | 本地 `pnpm dev` 能聊天 |
| Demo A 功能 | 5.3.1 全部满足 |
| `README.md` | 含启动说明 + 两段差异说明 |
| Git 提交（可选） | 至少 2～3 个有意义 commit，勿含 `.env` |

### 5.5 验收标准（自测勾选）

- [ ] 问「北京天气怎么样」能触发 `getWeather` 并在 UI 看到 tool 卡片
- [ ] 问「我屏幕多宽」能触发客户端 tool 并显示宽高
- [ ] 长回复过程中点 Stop，流立即停止
- [ ] 刷新页面后理解：会话是否持久化（本阶段 **可不持久化**，但 README 里说明）
- [ ] 能打开 React DevTools，指出 `MessageList` / `ToolCallCard` 拆分
- [ ] **不能**在浏览器 Network 里看到 API Key

### 5.6 常见卡点

| 现象 | 处理 |
| ---- | ---- |
| `Tool invocation must have a result` | 客户端 tool 忘了 `addToolOutput`；查 [排错文](https://ai-sdk.com.cn/docs/troubleshooting/tool-invocation-missing-result)（阶段 2 会再用） |
| 流式不出字 | 检查 route 是否 `return result.toDataStreamResponse()`（以当前 SDK 文档为准） |
| tool 只执行不显示 | 检查是否遍历 `message.parts` 而非只渲染 `content` |
| 中文站与代码不一致 | 对照 https://ai-sdk.dev/ 英文站同页 |

### 5.7 与后续阶段的关系

| Demo A 能力 | 阶段 2 / 3 怎么用 |
| ----------- | ----------------- |
| `parts` + ToolCallCard | Demo B 加 ApprovalCard；Demo C 加 PreviewCard |
| 服务端 / 客户端 tool | Demo C 的 `validateFields` 服务端、`previewNotification` 可客户端渲染 |
| Zod | Demo C `copy-schema` 包 |

---

## 6. 阶段 2：HITL + Generative UI（约 1.5～2 周）

**本阶段目标**：在 Demo A 基础上演进为 **Demo B**——危险操作要人确认；用 Generative UI 做 **通知预览卡**（假数据）。

### 6.1 要读什么

| 主题 | 读法 | 链接 |
| ---- | ---- | ---- |
| Human-in-the-Loop | **精读 + 照做** | https://ai-sdk.com.cn/cookbook/next/human-in-the-loop |
| Generative UI | **精读** | https://ai-sdk.com.cn/docs/ai-sdk-ui/generative-user-interfaces |
| Tool 无 result | **遇到错误时查** | https://ai-sdk.com.cn/docs/troubleshooting/tool-invocation-missing-result |

### 6.2 要做什么（按顺序）

- [ ] **Fork 或分支**：从 Demo A 拉出 `demo-b` 分支（仍在练习仓库）
- [ ] 跑通官方 HITL 最小示例（可单独路由，再合并进主 Chat）
- [ ] 新增 **写操作 tool**（二选一或都要）：
  - `publishCopy`：模拟「发布文案」
  - `sendTestEmail`：模拟「试发邮件」
- [ ] 上述 tool 必须 **`requireApproval`** 或等价 HITL 流程：UI 出确认卡 → 用户点批准/拒绝 → 再产生 tool-result
- [ ] 新增 **Generative UI**：`previewNotification` 返回结构化数据，前端渲染 **通知预览卡**（标题、正文、CTA 按钮文案——全部用假数据）
- [ ] 实现 **Retry**：失败后允许用户编辑上一条消息或点重试
- [ ] 写 `docs/demo-b-state-machine.md`：状态枚举 + 转移表

### 6.3 UI 方案（二选一，Week 3 前定稿）

| 方案 | 做法 | 链接 |
| ---- | ---- | ---- |
| A. Ant Design 自研 | `List` + `Card` + `Modal` 做 Thread；贴近运营后台 | https://ant.design/index-cn |
| B. assistant-ui | 用现成 Thread 组件，少写布局 | https://www.assistant-ui.com/docs/ |

建议：你熟悉 AntD → 选 A；想省时间 → 选 B。

### 6.4 Demo B 状态机（必须在文档里画清）

```text
idle
  → streaming（assistant 生成中）
  → tool_pending（tool 参数流式到达）
  → await_approval（写操作，等人点确认）
  → tool_executing
  → done | error
```

**读操作 tool**（如 `getCopySchema`）可跳过 `await_approval`。  
**写操作 tool** 必须经过 `await_approval`。

### 6.5 通知预览卡（假数据规格）

预览卡 UI 至少包含：

| 字段 | 示例（假） |
| ---- | ---------- |
| `trigger_type_code` | `bounty_review_rejected` |
| `title` | 图纸审核未通过 |
| `body` | 您提交的「桌面收纳盒」未通过审核… |
| `cta_label` | 查看详情 |
| `locale` | `zh-CN` |

数据来自 tool result，**不要**写死在公司仓库的 `triggerTypes.ts` 里拷贝。

### 6.6 交付物

| 交付物 | 说明 |
| ------ | ---- |
| Demo B 可运行 | HITL + 预览卡 |
| `docs/demo-b-state-machine.md` | 状态机说明 |
| `components/chat/ApprovalCard.tsx` | 确认卡组件 |
| `components/chat/NotificationPreviewCard.tsx` | Generative UI 预览 |

### 6.7 验收标准

- [ ] 触发 `publishCopy` 或 `sendTestEmail` 时 **不会** 未经确认直接 success
- [ ] 点「拒绝」后模型收到失败 result，能继续对话
- [ ] `previewNotification` 在聊天流里渲染出卡片，不是纯 Markdown
- [ ] 能对照状态机文档说出当前 UI 处于哪一态

---

## 7. 阶段 3：Demo C（当前业务域 · 约 2.5～3 周）

**本阶段目标**：**Notification Copy Agent Workbench**——用自造 trigger 与假文案，走完「查类型 → 填参数 → 校验 → 生成文案 → 预览 → 导出 snippet」。

### 7.1 题目说明

**为什么选这个**

- 正在做通知 / locale / 邮件，域模型熟，不需要过往公司代码
- 业务场景真实，练习价值高于玩具 Demo
- 用自造 fixture 即可，合规可控

**本阶段仍然不做**

- 不接 `web-community-platform` 真实 API
- 不拷贝公司 `triggerTypes` 全文、真实邮件 HTML、真实用户数据

### 7.2 用户故事（按此验收）

运营同学（你扮演）在/workbench 输入：

> 「帮我写赏金驳回通知，设计名是桌面收纳盒，原因是刀路不清晰」

系统应能：

1. 调 `listTriggerTypes` / `getCopySchema` 找到类型与必填字段  
2. 调 `validateFields` 检查 `reject_reason` 等是否齐全  
3. 生成中英文案（模型生成 + tool 约束）  
4. 调 `previewNotification` 出预览卡  
5. 调 `exportLocaleSnippet` 输出可粘贴的 `zh-CN` / `en-US` 片段  
6. 若有「发布」类动作 → **HITL**（继承 Demo B）

### 7.3 建议 Tools（全部自造实现）

| Tool | 执行端 | 作用 | 输入要点 | 输出要点 |
| ---- | ------ | ---- | -------- | -------- |
| `listTriggerTypes` | 服务端 | 列触发类型 | 可选分类 filter | `{ items: [{ code, description }] }` |
| `getCopySchema` | 服务端 | 字段 schema | `trigger_type_code` | Zod 可转的 fields 列表 |
| `validateFields` | 服务端 | 必填/格式 | `code` + `fields` | `{ ok, missing[], errors[] }` |
| `previewNotification` | 客户端 UI | 预览卡 | 文案 + code | 渲染 NotificationPreviewCard |
| `exportLocaleSnippet` | 服务端 | 导出片段 | locale + 文案对象 | 字符串 snippet |

可选加分：

- `checkEmailTemplateVars`：对照假 HTML 模板检查 `{{var}}` 是否齐  
- `sendTestEmail` + HITL：仅模拟，不真发信

### 7.4 与当前工作的映射（理解用）

| 工作里熟悉的 | Demo C 对应 |
| ------------ | ------------- |
| `trigger_type_code` 枚举 | `fixtures/triggers.json` 自造 10～15 条 |
| `business_params` 表单校验 | `validateFields` + `packages/copy-schema` |
| C 端通知列表展示 | `previewNotification` 预览卡 |
| `zh-CN` / `en-US` locale 文件 | `exportLocaleSnippet` |

### 7.5 仓库结构（本阶段补齐）

```text
agent-lab/
  apps/web/                    # 若 monorepo；单仓则保持 src/app 即可
  packages/agent-ui/           # Message / ToolCard / ApprovalCard / PreviewCard
  packages/copy-schema/        # Zod schema + 自造 trigger 定义
  fixtures/
    triggers.json              # 假 trigger 列表
    email-templates/           # 假 HTML，变量名自造
  evals/cases.json             # 阶段 4 填充，本阶段可先建空文件
  docs/
    architecture.md
    product.md
  README.md
```

### 7.6 分周任务（阶段 3 内部）

| 周 | 任务 | 勾选 |
| -- | ---- | ---- |
| W3 | 写 `docs/product.md` 一页需求；`fixtures/triggers.json` 10+ 条；`copy-schema` 包 2～3 个 trigger 的 Zod | [ ] |
| W4 | 实现 5 个 tool；打通「问一句 → 预览卡」主路径；写操作走 HITL | [ ] |
| W4 末 | `architecture.md` 初稿：Browser → Route Handler → Tools → Fixtures | [ ] |

### 7.7 架构示意

```text
Browser UI（useChat / AntD Thread / assistant-ui）
    ↓ UI Message Stream
Route Handler（streamText + tools）
    ↓
Tools：schema / validate / preview / export
    ↓
Fixtures（自造 trigger 与假文案；禁止公司私有文案与密钥）
```

### 7.8 合规（硬性）

- **不要**把公司私有文案、真实用户数据、未开源配置推到公开仓库
- 自造枚举与假数据；README 可写 `Inspired by notification workflows`
- API Key 仅服务端 `.env.local`
- **不依赖、不复刻过往公司任何项目代码**

### 7.9 交付物

| 交付物 | 说明 |
| ------ | ---- |
| Demo C 可演示 | 完整走通 7.2 用户故事 |
| `fixtures/` | 自造数据 |
| `packages/copy-schema/` | Zod |
| `docs/product.md` + `docs/architecture.md` | 需求与架构 |

### 7.10 验收标准

- [ ] 至少 **3 种** 不同 `trigger_type_code` 能完成预览 + 导出
- [ ] 故意少填 `reject_reason` 时 `validateFields` 能拦住并提示
- [ ] 全流程无公司真实数据；README 有合规说明
- [ ] 能 **5 分钟** 演示：输入 → tool 链 → 预览 → 复制 snippet

### 7.11 README 说明模板

> 基于 AI SDK 实现通知文案 Agent 工作台：流式对话、Tool Calling、HITL 与 Generative 预览；Zod schema + eval；覆盖「查类型→填参数→生成文案→预览→导出」链路。数据均为自造 fixture，Inspired by notification workflows.

---

## 8. 阶段 4：工程化、MCP、文档收尾（约 1.5～2 周）

### 8.1 评测（必做）

**要做什么：**

- [ ] 新建 `evals/cases.json`，至少 **20 条** case，覆盖：
  - 工具选对（该调 `validateFields` 时不要只聊天）
  - 缺字段拦截
  - 多语言导出
  - 错误 trigger_code
- [ ] 写 `evals/run.mjs`（或 `run.ts`）脚本，输出统计：
  - 工具选对率
  - 校验通过率
  - 平均对话步数
  - 失败类型分布
- [ ] 把结果数字写进根 `README.md` 的 **Eval** 小节

**参考：**

- LangSmith: https://docs.smith.langchain.com/  
- Prompt engineering: https://ai-sdk.com.cn/docs/ai-sdk-core/prompt-engineering  

### 8.2 MCP（必做：小实验 + 短文）

**要做什么：**

- [ ] 阅读 MCP 与 Function Calling 区别（中文链接见 8.2 原表）
- [ ] 按 https://ai-sdk.com.cn/docs/ai-sdk-core/mcp-tools 接 **1 个只读** MCP tool（或 mock server）
- [ ] 在 `docs/mcp-notes.md` 写 **半页**：Chat 内 tool vs MCP 的适用场景

**不要求**：把 Demo C 全部改成 MCP。

### 8.3 LangGraph（选修）

仅当 Demo C 出现「多步审批图」需求时再学：

- https://docs.langchain.org.cn/oss/javascript/langgraph/overview  
- https://docs.langchain.org.cn/oss/javascript/langgraph/use-graph-api  

### 8.4 安全（必做阅读 + README 一节）

- [ ] 扫一眼 OWASP LLM Top 10（中文解读搜即可；英文 https://owasp.org/www-project-top-10-for-large-language-model-applications/ ）
- [ ] README 写清：**工具白名单、写操作 HITL、密钥不进浏览器、fixture 脱敏**

### 8.5 交付物

| 交付物 | 说明 |
| ------ | ---- |
| `evals/cases.json` + 脚本 | 20+ case，有数字 |
| `docs/mcp-notes.md` | MCP 对比笔记 |
| README 终版 | 启动、架构、eval、合规、演示步骤 |
| 可选：录屏 3～5 分钟 | 本地演示 Demo C |

### 8.6 验收标准

- [ ] README 陌生人能按文档 `pnpm dev` 跑起来
- [ ] eval 脚本能在本机跑出 4 项统计
- [ ] 能回答：若把 Demo C 试点进真实运营后台，你会限制哪些 tool、哪些必须 HITL

---

## 9. 补充阅读

### 9.1 高优先级（中文）

- Anthropic Agent 模式全译: https://blog.iaieye.com/posts/agentic-coding-classics/anthropic-building-effective-agents-fulltext/  
- AI SDK Examples 中文: https://ai-sdk.com.cn/examples  

### 9.2 产品向

- Cursor 文档（英文为主，可浏览器翻译）: https://cursor.com/docs  

### 9.3 模型 API（中文优先）

- OpenAI 中文文档: https://developers.openai.ac.cn/api/docs/overview  
- Anthropic 文档（英文为主）: https://docs.anthropic.com/ 

### 9.4 明确不做

- 再系统学 React/TS
- 并行多个 Agent 框架
- 复刻过往物流/样板间系统
- 从零训模型

---

## 10. 按周清单（可打印勾选）

### Week 1 — 概念 + Demo A

- [ ] **D1-D2** 阶段 0 笔记 `notes/phase-0-concepts.md` + 时序图
- [ ] **D1** 练习仓库初始化（2.4）
- [ ] **D1-D3** Getting Started 流式对话跑通
- [ ] **D3-D5** Chatbot Tool Usage：`parts` + 双 tool
- [ ] **D5-D7** Stop + `MessageList` / `ToolCallCard` + Zod
- [ ] Demo A 对照 **5.5 验收标准** 全部勾选

### Week 2 — Demo A 文档 + Demo B 起步

- [ ] Demo A `README.md` 写完（5.3.4 三节）
- [ ] HITL cookbook 精读并跑通最小例
- [ ] Demo B：`ApprovalCard` + 危险 tool 一个
- [ ] Demo B：`NotificationPreviewCard` 初版（假数据）

### Week 3 — Demo B 收尾 + Demo C 准备

- [ ] `docs/demo-b-state-machine.md`
- [ ] Demo B 验收（6.7）
- [ ] `docs/product.md` + `fixtures/triggers.json`
- [ ] `packages/copy-schema` 起步
- [ ] 选定 UI：AntD 或 assistant-ui

### Week 4 — Demo C 主路径

- [ ] 5 个业务 tool 实现
- [ ] 主路径：问一句 → 校验 → 预览 → export snippet
- [ ] 写操作 HITL 接入
- [ ] `docs/architecture.md` 初稿

### Week 5 — Eval + 体验

- [ ] `evals/cases.json` 20+ 条 + 跑分脚本
- [ ] 错误态 / 超时态 / 空 trigger 提示
- [ ] `architecture.md` + `product.md` 定稿

### Week 6 — MCP + 收尾

- [ ] MCP 小实验 + `docs/mcp-notes.md`
- [ ] README 终版（含 eval 数字、合规、演示步骤）
- [ ] 可选：本地演示录屏
- [ ] 对照 **第 11 节** 自测 10 题

### Week 7～10 — 缓冲与加深

- [ ] 补薄弱章节（Generative UI / eval / MCP 任选）
- [ ] 技术笔记 1 篇（个人笔记，勿含公司隐私）
- [ ] 复盘 Demo C：若接真实业务会改哪 3 处

---

## 11. 理解自测清单

用来检查是否真正掌握，不必对外分享：

1. 流式半截 tool args 如何渲染？→ https://ai-sdk.com.cn/docs/ai-sdk-ui/chatbot-tool-usage  
2. Stop / 进行中 tool 如何处理？→ https://ai-sdk.com.cn/docs/advanced/stopping-streams  
3. tool 无 result 为何失败？→ https://ai-sdk.com.cn/docs/troubleshooting/tool-invocation-missing-result  
4. HITL 状态机？读操作 vs 写操作门槛？→ https://ai-sdk.com.cn/cookbook/next/human-in-the-loop  
5. Generative UI 与 Markdown 边界？如何版本化 UI 协议？→ https://ai-sdk.com.cn/docs/ai-sdk-ui/generative-user-interfaces  
6. 密钥与权限边界？  
7. 通知配置为何不全做成 Agent？表单 / 审核流 / Agent 如何分工？→ Agent 模式译文「何时不用 Agent」  
8. 如何做 eval？Prompt/Tool schema 变更如何回归？  
9. 成本与延迟如何分层？  
10. 中后台复杂状态 / 权限经验，如何迁移到 Agent 工具设计？（只谈通用方法）  

---

## 12. 完成标准（全方案）

1. 有可运行的 **Demo C**（当前业务域脱敏），结构清晰 + 合规说明
2. 能讲清消息流、tool 状态机、HITL
3. 有 **eval 数字**（20+ case）
4. README 能说明设计取舍与边界
5. 能提出「通知/邮件场景试点」的思路（范围、权限、风险）——仅方案级，不要求落地公司仓库

---

## 13. 链接速查（中文优先）

### 核心（AI SDK 中文站）

- 首页: https://ai-sdk.com.cn/  
- 快速开始: https://ai-sdk.com.cn/docs/getting-started  
- 工具调用: https://ai-sdk.com.cn/docs/ai-sdk-core/tools-and-tool-calling  
- Chatbot Tool UI: https://ai-sdk.com.cn/docs/ai-sdk-ui/chatbot-tool-usage  
- HITL: https://ai-sdk.com.cn/cookbook/next/human-in-the-loop  
- Generative UI: https://ai-sdk.com.cn/docs/ai-sdk-ui/generative-user-interfaces  
- 停止流: https://ai-sdk.com.cn/docs/advanced/stopping-streams  
- MCP 接入: https://ai-sdk.com.cn/docs/ai-sdk-core/mcp-tools  

### 协议与通识（中文）

- OpenAI 函数调用: https://developers.openai.ac.cn/api/docs/guides/function-calling  
- Anthropic Agent 全译: https://blog.iaieye.com/posts/agentic-coding-classics/anthropic-building-effective-agents-fulltext/  
- 工具调用与 MCP 通识: https://developer.volcengine.com/articles/7533551311816818724  
- MCP vs Function Calling: https://cloud.tencent.com/developer/techpedia/2621/20674  
- LangGraph 中文: https://docs.langchain.org.cn/oss/javascript/langgraph/overview  

### 基础（中文）

- Next.js 中文: https://nextjs.net.cn/docs/app  
- Zod 中文: https://zod.dev.cn/  
- Ant Design 中文: https://ant.design/index-cn  
- AbortController: https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController  
- SSE: https://developer.mozilla.org/zh-CN/docs/Web/API/Server-sent_events  

### 英文原文（可选对照）

- AI SDK 官方: https://ai-sdk.dev/  
- Anthropic 原文: https://www.anthropic.com/engineering/building-effective-agents  
- MCP 官网: https://modelcontextprotocol.io/  
- Cursor Docs: https://cursor.com/docs 

---

## 14. 修订记录

| 日期       | 说明                                                                 |
| ---------- | -------------------------------------------------------------------- |
| 2026-08-20 | 初版与排版迭代；练习选题对齐当前业务域                               |
| 2026-08-20 | 阅读材料改为中文优先（AI SDK / OpenAI / Agent 译文 / LangGraph 等）  |
| 2026-08-24 | 各阶段补充「要做什么 / 交付物 / 验收标准」；阶段 1 日程与 Demo A 规格 |

---

_文档路径：`agent-lab/工程师学习.md`（本仓库根目录）_
